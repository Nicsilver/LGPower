<?xml version="1.0" encoding="utf-8"?>
<!--
    Foreground layer for the adaptive launcher icon.
    Safe zone is the centre 72 × 72 dp of the 108 × 108 dp canvas (i.e. inset 18 dp on each side).
    The power symbol is centred at (54, 54) and scaled to fill the safe zone comfortably.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Vertical stem -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:pathData="M54,27 L54,54" />

    <!-- Arc — large sweep leaves gap at top for the stem -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:pathData="M37,37 A24,24 0 1,0 71,37" />

</vector>
